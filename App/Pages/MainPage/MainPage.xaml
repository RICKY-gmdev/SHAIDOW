<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App.MainPage">
    <Grid>
        <ScrollView>

            <VerticalStackLayout Padding="30"
                                 Spacing="25"
                                 HorizontalOptions="Center">
                <Grid>
                    <Border BackgroundColor="Transparent"
                            Stroke="LightGray"
                            Padding="0"
                            HorizontalOptions="End"
                            VerticalOptions="Start">
                        <Button ImageSource="themeswitch.png"
                                HorizontalOptions="End"
                                VerticalOptions="Start"
                                WidthRequest="30"
                                HeightRequest="30"
                                BackgroundColor="Transparent"
                                Clicked="OnLightModeClicked"/>
                    </Border>
                </Grid>
                <!-- Logo -->
                <Image Source="shaidow.png"
                       WidthRequest="600"
                       HeightRequest="360"
                       Aspect="AspectFit"/>

                <!-- App Title -->
                <Border Stroke="#FF3F3D3D"
                        StrokeShape="RoundRectangle 20"
                        Padding="5"
                        BackgroundColor="Black"
                        WidthRequest="280"
                        HeightRequest="90"
                        HorizontalOptions="Center">
                    <Label Text="SHAIDOW"
                           TextColor="Cyan"
                           BackgroundColor="Black"
                           FontSize="{OnPlatform iOS=40, Android=40, MacCatalyst=40,WinUI=40}"
                           HorizontalTextAlignment="Center"
                           VerticalTextAlignment="Center"/>
                </Border>

                <!-- Chat List -->
                <ListView x:Name="ChatList"
                          ItemsSource="{Binding ChatMessages}"
                          HasUnevenRows="True"
                          BackgroundColor="Transparent"
                          SeparatorVisibility="None"
                          SelectionMode="None"
                          VerticalOptions="Fill">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <!-- This Border provides the simple, black bubble style you preferred -->
                                <Border Stroke="Black"
                                        StrokeShape="RoundRectangle 10"
                                        BackgroundColor="Black">
                                    <StackLayout Padding="10">
                                        <!-- Text View (Visible only if there is text) -->
                                        <Label Text="{Binding Text}"
                                               IsVisible="{Binding IsText}"
                                               TextColor="White"/>

                                        <!-- Image View (Visible only if there is an image URL) -->
                                        <Image Source="{Binding ImageUrl}"
                                               IsVisible="{Binding IsImage}"
                                               HeightRequest="256"
                                               Aspect="AspectFit"/>
                                    </StackLayout>
                                </Border>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- Input Box -->
                <HorizontalStackLayout HorizontalOptions="Center"
                                       Spacing="10">
                    <Entry x:Name="UserInput"
                           Placeholder="Ask SHAIDOW AI..."
                           WidthRequest="250"
                           VerticalOptions="Center"
                           TextColor="cyan"
                           Completed="OnSendMessage"/>
                    <Button Text="Send"
                            Clicked="OnSendMessage"/>
                    <ActivityIndicator x:Name="Spinner"
                                       IsVisible="False"
                                       IsRunning="False"
                                       WidthRequest="25"
                                       HeightRequest="25"
                                       TranslationX="0"
                                       HorizontalOptions="Start"
                                       VerticalOptions="Center"
                                       Color="Cyan"/>
                </HorizontalStackLayout>
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>